@model IEnumerable<iveCincoFrame.Models.TblBitacoraBusquedaUsuarios>

@{
    ViewBag.Title = "BusquedasLista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">

    <div class="card-header">
        <h1>Reporte de busquedas realizadas por usuario</h1>
    </div>
    <div class="card-body">

        <br />
        <br />

        <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="home-tab" data-bs-toggle="tab" data-bs-target="#home" type="button" role="tab" aria-controls="home" aria-selected="true">Completados</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile" type="button" role="tab" aria-controls="profile" aria-selected="false">Denegados</button>
            </li>
            <li class="nav-item d-none" role="presentation">
                <button class="nav-link" id="contact-tab" data-bs-toggle="tab" data-bs-target="#contact" type="button" role="tab" aria-controls="contact" aria-selected="false">Contact</button>
            </li>
        </ul>
        <div class="tab-content border px-3 py-2" id="myTabContent">
            <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
                @Html.Action("busquedasReporte")
                @*<table id="tabla-roles" class="table table-bordered table-hover table-striped" style="width:100%">
                        <thead>
                            <tr>
                                <th class="d-none">
                                    @Html.DisplayNameFor(model => model.IdRol)
                                </th>
                                <th>#</th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Descripcion)
                                </th>
                                <th>
                                    Acciones
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @{var iterador = 1;}
                            @foreach (var item in Model)
                            {

                                <tr>

                                    <td class="d-none">
                                        @Html.DisplayFor(modelItem => item.IdRol)
                                    </td>
                                    <td>@iterador</td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Descripcion)
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <button type="button" class="btn btn-success Accesos" codigo="@item.IdRol"><span><i class="align-middle me-2 fas fa-fw fa-lock"></i></span>Accesos</button>
                                            <button type="button" class="btn btn-danger eliminarRol" codigo="@item.IdRol"><span><i class="align-middle me-2 fas fa-fw fa-trash-alt"></i></span>Eliminar</button>
                                        </div>
                                    </td>
                                </tr>
                                iterador++;
                            }
                        </tbody>
                    </table>*@
            </div>

        </div>

    </div>
    <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab"></div>
    <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">...</div>
</div>
@model IEnumerable<iveCincoFrame.Models.tblbitacorapremora>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-12">
        <div>
            <div class="card">
                <div class="card-body">
                    <div class="my-3">

                        <div id="transac-directorios" class="col-12">
                            <h2>Index</h2>

                            <p>
                                @Html.ActionLink("Create New", "Create")
                            </p>
                            <table class="table">
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.fechayhorallamada)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.observacion)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TblUsuarios.usuario)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.catestadoscallcenter.descripcion)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.tblpremora.nombre)
                                    </th>
                                    <th></th>
                                </tr>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.fechayhorallamada)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.observacion)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TblUsuarios.usuario)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.catestadoscallcenter.descripcion)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.tblpremora.nombre)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Edit", "Edit", new { id = item.idtblbitacorapremora }) |
                                            @Html.ActionLink("Details", "Details", new { id = item.idtblbitacorapremora }) |
                                            @Html.ActionLink("Delete", "Delete", new { id = item.idtblbitacorapremora })
                                        </td>
                                    </tr>
                                }

                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



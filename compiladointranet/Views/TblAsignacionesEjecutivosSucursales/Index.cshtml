@model IEnumerable<iveCincoFrame.Models.TblAsignacionesEjecutivosSucursales>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-12">
        <div>
            <div class="card">
                <div class="card-body">
                    <div class="my-3">

                        <div id="transac-directorios" class="col-12">
                            <p>
                                @Html.ActionLink("Create New", "Create")
                            </p>
                            <table class="table">
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.TblUsuarios.usuario)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.CatUbicaciones.codigo)
                                    </th>
                                    <th></th>
                                </tr>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TblUsuarios.usuario)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.CatUbicaciones.codigo)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Edit", "Edit", new { id = item.idTblAsignacionesEjecutivosSucursales }) |
                                            @Html.ActionLink("Details", "Details", new { id = item.idTblAsignacionesEjecutivosSucursales }) |
                                            @Html.ActionLink("Delete", "Delete", new { id = item.idTblAsignacionesEjecutivosSucursales })
                                        </td>
                                    </tr>
                                }

                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


